apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: guestbook-frontend-route
spec:
  to:
    kind: Service
    name: guestbook-frontend-service  # Must match frontend service name exactly
  port:
    targetPort: 8080                  # The port frontend app is listening on